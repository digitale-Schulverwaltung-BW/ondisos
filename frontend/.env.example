# ============================================================================
# Frontend Environment Configuration
# ============================================================================
# Copy this file to .env and fill in your production values
# NEVER commit .env to git! (.gitignore should contain .env)
#
# Quick Setup:
#   cp .env.example .env
#   nano .env  # Edit values
#   chmod 600 .env  # Secure permissions
# ============================================================================

# ============================================================================
# BACKEND API
# ============================================================================
# Backend API URL (where form submissions are sent)
#
# Docker Frontend → Docker Backend:
#   BACKEND_API_URL=http://backend/api
#
# Manual Frontend → Docker Backend:
#   BACKEND_API_URL=http://intranet.example.com:8080/api
#
# Manual Frontend → Manual Backend:
#   BACKEND_API_URL=http://intranet.example.com/backend/api
#
BACKEND_API_URL=http://localhost/api

# ============================================================================
# EMAIL SETTINGS
# ============================================================================
# Email address for notification emails (sent to admins)
FROM_EMAIL=noreply@example.com

# Email header text
MAIL_HEAD=Eine neue Anmeldung ist eingegangen.

# Email footer text
MAIL_FOOT=Mit freundlichen Grüßen

# ============================================================================
# CORS CONFIGURATION
# ============================================================================
# Comma-separated list of allowed origins for API requests
# This is configured in the BACKEND .env, not here!
# The backend needs to whitelist frontend origins for CORS.

# ============================================================================
# FILE UPLOAD
# ============================================================================
# Max upload size in bytes (10485760 = 10MB)
# Should match or be lower than backend UPLOAD_MAX_SIZE
UPLOAD_MAX_SIZE=10485760

# Allowed file types (comma-separated)
# Common types: pdf, jpg, jpeg, png, gif, doc, docx, xls, xlsx
# Security: Only allow necessary types!
UPLOAD_ALLOWED_TYPES=pdf,jpg,jpeg,png,gif,doc,docx

# ============================================================================
# PRODUCTION CHECKLIST
# ============================================================================
# Before deploying to production, ensure:
# [ ] BACKEND_API_URL points to production backend
# [ ] FROM_EMAIL is a valid email address
# [ ] ALLOWED_ORIGINS configured in BACKEND .env (not here!)
# [ ] UPLOAD_ALLOWED_TYPES contains only necessary file types
# [ ] .env file has chmod 600 permissions
# [ ] .env is in .gitignore (never commit!)
# ============================================================================